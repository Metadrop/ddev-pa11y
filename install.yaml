name: pa11y

project_files:
  - docker-compose.pa11y.yaml
  - commands
  - pa11y
  - pa11yBuild

post_install_actions:
  - echo "Post install started..."
  - echo "Creating tests folders"
  - test -d ${DDEV_APPROOT}/tests/pa11y/local || mkdir -p ${DDEV_APPROOT}/tests/pa11y/local
  - test -d ${DDEV_APPROOT}/reports/pa11y || mkdir -p ${DDEV_APPROOT}/reports/pa11y
  - touch ${DDEV_APPROOT}/reports/pa11y/.gitkeep
  - test -f ${DDEV_APPROOT}/tests/pa11y/local/config.json || cp pa11y/config.json ${DDEV_APPROOT}/tests/pa11y/local/config.json
  - test -f ${DDEV_APPROOT}/tests/pa11y/local/pa11yci.json || cp pa11y/pa11yci.json ${DDEV_APPROOT}/tests/pa11y/local/pa11yci.json
  - echo "Install finished."
